# registry

Metadata registry driving frontend UI copy, the model picker, and the AI
interpreter's prompt context. See [Concepts § The registry](../concepts.md#the-registry).

::: climate_change.registry
    options:
      members:
        - ModelOption
        - UseCaseInfo
        - USE_CASE_REGISTRY
        - get_use_case_info
